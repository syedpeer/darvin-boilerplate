'use strict';

// Gernerated one instance of the scoped pubsub system, which can be loaded everywhere and ou can communicate from every component to every component using this object
import observer from '../libs/observer';

export default observer();